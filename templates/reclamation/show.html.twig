{% extends 'dashboard.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ reclamation.contenu }}</td>
            </tr>
                 <td><img src="{{ asset('uploads/' ~reclamation.image) }}" style="width:100px; height:100px;" alt=""></td>
                 </tr>
            <tr>
                <th>Reclamation Associée</th>
            <td>{{ reclamation.getIdReservation().getId() }}</td>
            </tr>
        </tbody>
    </table>

<a href="{{ path('app_reclamation_show', {'id': reclamation.id}) }}">back to list</a>

    <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}">edit</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
